# 产品需求文档：Rigel 实时网络流量调度系统 - V1.1

## 1. 修订历史
| 版本号 | 修订日期   | 修订人 | 修订内容 |
| ------ | ---------- | ------ | -------- |
| V1.0   | 2025-7-15 | 陈闯  | 基于学术论文资料创建初稿。 |
| V1.1   | 2025-7-28 | 陈闯  | 根据“逐块决策、整路下发”的新流程，重构核心工作流、组件职责和相关功能点，明确了Client模块的角色。 |

## 2. 项目背景与目标
### 2.1 核心问题
在现代多云（Multi-Cloud）和异构网络环境下，传统的网络流量调度系统面临巨大挑战。它们难以应对实时变化的流量需求和不稳定的网络状况，导致吞吐量低、延迟高、资源利用率不足。现有工具对于纯粹的流量转发场景过于笨重，且无法实现跨服务商的智能路由优化。

本项目旨在开发 **Rigel** 系统，一个分布式的、实时的网络流量调度系统，以解决上述问题。

### 2.2 需求目标
1.  **提升传输性能**: 在复杂的云网络中，实现高吞吐量、低成本的数据传输。
2.  **增强系统鲁棒性**: 能够实时适应网络环境的动态变化（如拥塞、链路质量波动），动态规划最优传输路径。
3.  **实现资源弹性**: 能够根据实时网络负载，自动伸缩云端代理节点资源，以最低成本应对流量高峰。
4.  **保证系统可扩展性**: 采用分布式架构，确保系统性能可以随着节点数量的增加而线性扩展，避免中心化瓶颈。

## 3. 核心组件与角色
### 3.1 Client 模块
*   **部署位置**: 部署在发送方（Sender）主机上，作为 Rigel 系统的入口。
*   **核心职责**:
    1.  作为数据传输任务的发起者，将大文件拆分为标准大小的数据块。
    2.  负责为待发送的**每一个数据块**向网络中的决策节点请求路由。
    3.  将获取到的**完整路径**和**数据块内容**进行封装。
    4.  根据获取到的**传输速率**指令，控制本地的发送行为（速率限制）。

### 3.2 代理节点 (ProxyAgent)
*   **部署位置**: 构成 Rigel 网络的核心基础设施节点，部署在全球各地的云服务器上。
*   **核心职责**:
    1.  **数据平面**: 作为一个高效的、被动的**执行者**。根据数据块头部封装的路径信息，进行“傻瓜式”的接力转发。
    2.  **控制平面**: 作为一个分布式的**决策者**。运行控制平面模块，响应 Client 的路由请求，并参与全网状态同步。

## 4. 系统工作流程详述
本系统采用“逐块决策、整路下发、分段执行”的动态路由模型。

1.  **背景任务：全网状态同步**
    *   每个代理节点上的控制平面模块，持续地向其所在区域的组长节点（手动指定）上报自身状态（如负载、链路质量、虚拟队列状态等）。
    *   组长节点之间互相交换汇总后的区域信息。
    *   组长节点再将全局信息同步给区域内的所有节点。
    *   **最终结果**: 每个代理节点都拥有一份准实时的、相同的全局网络状态“地图”，为其路径计算提供数据基础。

2.  **核心任务：单个数据块的传输（循环执行）**
    *   **Step 1: 请求决策**: Sender 上的 **Client 模块**为待发送的单个数据块，选择一个代理节点（例如通过延迟探测选择最近的）作为决策点，并发起路由请求。请求中包含任务目的地等元数据。
    *   **Step 2: 路径与速率计算**: 被选中的代理节点上的 **`TrafficScheduler`** 模块，利用其本地的全局网络“地图”，执行基于UMW框架的优化算法，计算出一条**完整的、端到端的转发路径**和**建议的最优传输速率**。
    *   **Step 3: 返回决策**: 决策节点将计算出的“完整路径”和“传输速率”返回给 Client 模块。
    *   **Step 4: 封装与发送**: **Client 模块**将“完整路径”封装进数据块的头部，并根据“传输速率”指令配置本地的速率控制器（如令牌桶），然后将数据块发送给路径的第一个代理节点。
    *   **Step 5: 接力转发**: 路径上的每个代理节点，只需解析数据块头部的路径信息，确定下一跳地址，然后进行转发。此过程极其轻量，不涉及任何本地决策或外部查询。
    *   **Step 6: 循环**: Client 模块为下一个数据块重复从 Step 1 开始的整个流程，从而为每个数据块获取基于最新网络状况的独立路由。

## 5. 功能需求详述

### 5.1 功能模块一：Client 模块
#### 5.1.1 功能点：路由决策请求
*   **作为** 任务发起者，
*   **我希望** 能向指定的 Rigel 代理节点发起请求，
*   **以便于** 为每个待发送的数据块获取最优的转发路径和传输速率。
*   **业务规则与逻辑**:
    1.  Client 应能配置一个或多个决策节点的地址列表。
    2.  在发起请求时，应能从列表中选择一个决策节点（例如通过轮询或延迟探测）。
    3.  请求中需包含任务元数据（如最终目的地、任务优先级等）。
*   **验收标准**:
    1.  能够成功向代理节点发送路由请求并接收响应。
    2.  响应中包含结构化的路径信息（节点地址列表）和速率信息（如 bps）。

#### 5.1.2 功能点：数据封装与速率控制
*   **作为** 数据的发送端，
*   **我希望** 能将获取到的路径信息封装到数据块头部，并遵守获取到的速率指令，
*   **以便于** 让代理网络能正确转发数据，并避免造成网络拥塞。
*   **业务规则与逻辑**:
    1.  需定义清晰的数据块头部格式（应用层协议），用于存放有序的路径节点列表及其他元数据。
    2.  必须实现一个速率控制机制（如令牌桶），并能根据收到的指令动态调整其速率。
*   **验收标准**:
    1.  发送出的数据块头部包含正确的路径信息。
    2.  实际发送速率与指令速率的误差在可接受范围内（如5%）。

### 5.2 功能模块二：代理节点 - 控制平面

### 5.2.1 功能点：基于组长节点的中心化状态同步

*   **作为** Rigel 网络中的一员，
*   **我希望** 能通过与本区域的组长节点（Leader）进行 API 交互，实现状态的上报与全局视图的获取，
*   **以便于** 在不引入外部协调服务（如etcd）的情况下，高效地维护一份跨节点一致的全局网络状态。

*   **业务规则与逻辑**:
    1.  **组长节点作为信息中心**:
        *   组长节点必须在内存中维护一个**实时的成员列表**，记录区域内所有普通节点的地址、健康状况和详细状态。
        *   组长节点还必须维护一个**全局网络状态视图**，该视图由本区域的详细信息和从其他组长同步来的摘要信息构成。
    2.  **区域内同步：通过 API 实现**
        *   **状态上报 (Push)**: 普通节点必须周期性地（如每5秒）调用组长节点提供的 `POST /api/v1/nodes/{node_id}/status` 接口，上报自身最新的状态信息。上报内容需包含：
            *   CPU/内存使用率。
            *   出/入带宽。
            *   **关键指标：本地所有出向链路的虚拟队列状态 `Õe(t)`**。
        *   **心跳维持**: 上述状态上报接口同时作为**心跳机制**。如果组长在指定时间内（如30秒）未收到某节点的上报，则判定该节点为失联状态，并将其从健康成员列表中移除。
        *   **全局视图获取 (Pull)**: 所有节点（包括组长自己）需周期性地（如每1秒）调用组长节点提供的 `GET /api/v1/global-status` 接口，获取由组长整合好的、最新的全局网络状态视图。
    3.  **跨区域同步：组长间的 API 交互**
        *   组长节点之间需周期性地（如每1秒）互相调用对方的 `GET /api/v1/region-summary` 接口，获取对方区域的**状态摘要**（例如，区域间的链路质量、总体负载等）。
        *   获取到其他区域的摘要后，组长节点负责将其整合进自己的全局网络状态视图中，并通过 `GET /api/v1/global-status` 接口提供给区域内的所有节点。
    4.  **数据结构**: 必须定义清晰、统一的 JSON 或 Protobuf 格式，用于 API 请求体和响应体中的状态数据交换。

*   **验收标准**:
    1.  在组长节点上，可以通过一个管理接口（如 `GET /api/v1/region-nodes`）实时查看到区域内所有健康节点的列表及其详细状态。
    2.  一个普通节点下线后，在心跳超时时间内，它应从组长的健康成员列表中被移除。
    3.  一个普通节点上报的状态变化（如某个虚拟队列值激增），能在下一个“全局视图获取”周期内，被区域内所有其他节点通过 API 获取到。
    4.  一个区域的摘要信息变化，能在1-2个“跨区域同步”周期内，反映到其他所有区域的全局视图中。

### 5.2.2 功能点：基于 UMW 的在线路由优化服务 (Online Routing via UMW)

*   **作为** 一个可被 Client 选择的决策节点，
*   **我希望** 能提供一个服务接口，该接口实现了基于**通用最大权重（UMW）框架**和 **Drift-Plus-Penalty** 优化的在线路由算法，
*   **以便于** 为 Client 的数据块传输请求，实时计算出能最大化**净效用（Net Utility）**并保证**网络稳定性（Stability）**的最优路径和传输速率。

*   **业务规则与逻辑**:
    1.  **核心优化目标**: 算法的根本目标是解决论文中的优化问题 **(P)**，即在保证网络稳定性（公式7）的前提下，最大化系统的长期平均净效用（公式6）。
    2.  **算法实现**: 必须实现论文中 **Algorithm 4.1 (Global UMW Optimizer)** 的两阶段逻辑，该算法是最小化 **Drift-Plus-Penalty (DPP)** 函数（公式10）的等效方法。
    3.  **网络模型抽象**:
        *   系统必须将物理网络抽象为一个扩展有向图 `G'=(V', E')`。
        *   为了将节点拥塞转化为链路拥塞，每个中间节点 `v` 必须在模型中被拆分为一个输入组件 `iv` 和一个输出组件 `jo`，并由一条虚拟边 `eo = (iv, jo)` 连接（参考 Figure 7a）。
        *   **虚拟队列 (Virtual Queue)** `Õe` 必须与图 `G'` 中的每条边 `e` 相关联，其状态演化遵循公式 (4) `Õe(t+1) = max[0, Õe(t) + ae(t) - be(t)]` 的动态。
    4.  **第一阶段：路由 (Routing)**
        *   **链路成本计算**: 对于扩展图 `G'` 中的每条边 `e`，其权重（成本）`We` 必须根据公式 `We = w_e^M * Õe(t) + V_M * p_e` 进行计算。
            *   `Õe(t)`: 从全局状态视图中获取的该边的实时虚拟队列值。
            *   `p_e`: 该边的固有单位传输成本（对于代表节点的虚拟边 `eo`，`p_e=0`）。
            *   `w_e^M`, `V_M`: 系统可配置的、用于平衡稳定性和效用的权重参数。
        *   **路径选择**: 在计算完所有边的权重后，必须使用标准的**最短路径算法**（如 Dijkstra）来计算从源 `s_k` 到目的地 `d_k` 的、总成本 `C*k(t)` 最低的完整路径 `T*k,*(t)`。
    5.  **第二阶段：分配 (Allocation)**
        *   **速率计算**: 在确定最优路径后，必须根据论文公式 (12) `a*k(t) = (V_M * w_k^U / C*k(t))^(1/α)` 计算出最优传输速率 `a*k(t)`。
        *   该计算必须支持可配置的**任务权重 `w_k^U`**（来自效用函数公式5）和**公平性参数 `α`**，以实现对不同任务的优先级（`w_k^U`）和公平性策略（`α=0`对应吞吐量最大化，`α→1`对应比例公平）的调控。
        *   **带宽约束**: 最终分配的速率还必须受路径上所有物理链路的最小带宽约束 `be(t)` 的限制。
    6.  **虚拟队列更新**: 每次路由决策（即一次流量分配）做出后，必须更新被使用路径 `T*k,*(t)` 上所有边的虚拟队列 `Õe(t)`，以体现本次决策对网络拥塞状态的影响，为下一次决策提供反馈。

*   **验收标准**:
    1.  API 能够接收包含任务目的地、优先级 `w_k^U`、公平性参数 `α` 等信息的请求，并返回结构化的路径（节点列表）和传输速率（bps）。
    2.  在模拟测试中，当某条边的虚拟队列 `Õe(t)` 被手动调高后，后续的路由请求能自动计算出成本更高的路径，并可能选择替代路径。
    3.  在模拟的持续高流量场景下，所有虚拟队列 `Õe(t)` 的长期平均值应保持有界（符合公式7的稳定性要求），不会无限增长。
    4.  通过调整参数 `α`，可以观察到系统在吞吐量和公平性之间的行为变化。例如，`α` 接近1时，低优先级任务也能获得一定的带宽；`α` 为0时，系统会倾向于将所有带宽分配给最高效的路径。
    5.  在给定输入（网络状态、任务参数）下，算法的计算结果（路径成本、分配速率）与手动代入公式计算的结果一致。

### 5.3 功能模块三：代理节点 - 数据平面
#### 5.3.1 功能点：基于路径的接力转发
*   **作为** 数据传输路径上的一个中继节点，
*   **我希望** 能高效地解析数据块头部的路径信息，并将其转发给路径中的下一个节点，
*   **以便于** 忠实地执行 Client 指定的转发任务。
*   **业务规则与逻辑**:
    1.  代理必须能从TCP流中解析出数据块的边界（Chunk Framing）。
    2.  代理必须能解析出头部封装的、有序的路径列表。
    3.  确定下一跳地址后，代理可以选择性地从路径列表中移除自身信息，再进行转发。
    4.  转发逻辑应极其轻量，不涉及任何本地决策或外部查询。
    5.  **数据缓冲机制**: 代理节点应首先将接收到的数据放入缓冲区，然后从缓冲区中取出完整的数据块进行解析和转发处理，以确保数据完整性和处理效率。
*   **验收标准**:
    1.  能够正确解析路径并转发到下一跳。
    2.  数据转发的延迟（Processing Delay）极低。
    3.  缓冲区能够正确处理TCP流的分片和重组。

### 5.4 功能模块四：基于反压的弹性伸缩 (Elastic Scaling through Backpressure)

#### 5.4.1 功能点：基于波动性队列的伸缩决策 (Volatility-Aware Scaling Decision)

*   **作为** 系统的资源管理者 (`ElasticController`)，
*   **我希望** 能实现一个基于**反压模型（Backpressure Model）**的伸缩器（BP-Scaler），该伸缩器使用“波动性队列”来量化伸缩需求，
*   **以便于** 做出稳定、高效且能抑制系统抖动的弹性伸缩决策，而不是简单地响应瞬时拥塞。

*   **业务规则与逻辑**:
    1.  **波动性队列 (Volatility Queue) `Zi(t)`**:
        *   为每个节点 `i` 维护一个**波动性队列 `Zi(t)`**，用于捕捉其关联的**虚拟队列 `Õi(t)`** 的短期剧烈变化。
        *   必须首先计算**单时隙扰动 `Pi(t)`**（公式16），它量化了虚拟队列 `Õi(t)` 超过某个阈值 `CV` 后的增量。
        *   波动性队列 `Zi(t)` 的状态必须根据公式 (17) `Zi(t+1) = max[0, γi*Zi(t) + w_i^V*Pi(t) - Ci(t)]` 进行演化。其中 `γi` 是衰减因子，`w_i^V` 是敏感度权重，`Ci(t)` 是稳定能力。
    2.  **伸缩决策算法 (BP-Scaler)**:
        *   必须实现论文中的 **Algorithm 5.1 (BP-Scaler)**。
        *   在每个决策周期，算法需计算一个决策变量 `Δi(t) = -γi*w_i^S*Zi(t)*Pi(t) + V_S*Cost_i(t,s)`。
        *   **决策规则**: 如果 `Δi(t) > 0`，则做出伸缩决策 `ui(t) = 1`（即触发伸缩）；否则 `ui(t) = 0`（保持不变）。
    3.  **成本模型 `Cost_i(t,s)`**:
        *   伸缩成本的计算必须区分两种情况（公式19）：
            *   **初次启动 (INITIATE)**: 成本为 `C_fixed,i + βi*Pi(t)`，包含一次性的虚拟机启动成本 `C_fixed,i`。
            *   **从休眠唤醒 (DORMANT)**: 成本为 `βi*Pi(t)`，不含固定启动成本，因为资源已预分配。
        *   这要求系统能够识别并管理节点的这两种不同状态。

*   **验收标准**:
    1.  可以查询到每个节点实时的波动性队列 `Zi(t)` 和扰动 `Pi(t)` 的值。
    2.  在模拟测试中，当一个节点的虚拟队列 `Õi(t)` 持续剧烈增长时，其 `Zi(t)` 也随之增长，并最终触发伸缩决策 `ui(t) = 1`。
    3.  当一个节点的虚拟队列恢复平稳后，其 `Zi(t)` 会因衰减因子 `γi` 的作用而逐渐下降。
    4.  在相同的扰动 `Pi(t)` 下，处于 DORMANT 状态的节点比处于 INACTIVE 状态的节点更容易被触发伸缩（因为其 `Cost_i` 更低）。

#### 5.4.2 功能点：有状态的资源生命周期管理 (Stateful Resource Lifecycle Management)

*   **作为** 弹性资源的管理者，
*   **我希望** 能实现一个**有状态的资源保留机制**，通过一个明确的状态机来管理弹性伸缩出的节点的完整生命周期，
*   **以便于** 平衡资源利用率和响应速度，避免因云厂商分钟级的启动延迟而导致决策失效，并防止资源的过早释放。

*   **业务规则与逻辑**:
    1.  **节点状态机实现**:
        *   必须实现论文中 **Figure 8** 所示的**弹性资源状态转换图**。
        *   每个节点资源状态 `si(t)` 必须能被明确地分为两大类六小类：
            *   **UNSCALED (未伸缩)**: `INACTIVE`, `SCALING_UP`, `RELEASING`。
            *   **SCALED (已伸缩)**: `DORMANT`, `TRIGGERED`, `PERMANENT`。
    2.  **状态转换逻辑**:
        *   当伸缩决策 `ui(t) = 1` 做出后：
            *   如果节点原状态为 `INACTIVE`，则进入 `SCALING_UP` 状态，并由 `ScalerWorker` 调用云API创建VM。VM启动完成后，进入 `DORMANT` 状态。
            *   如果节点原状态为 `DORMANT`，则立即进入 `TRIGGERED` 状态，激活其预分配的容量。
    3.  **资源保留时间 (Retain Time)**:
        *   必须实现一个**指数衰减的保留时间计算策略**（公式22, 23）。
        *   节点的保留时间 `ri(t)` 由一个基础时间 `T0` 和一个基于近期活动历史的“活动分 `activity_score`”共同决定。
        *   `activity_score` 是近期所有 `TRIGGERED` 事件的指数衰减加权和。
    4.  **从活跃到休眠/释放**:
        *   当一个 `TRIGGERED` 状态的节点不再活跃时，它会转换回 `DORMANT` 状态，并开始计算其保留时间 `ri(t)`。
        *   在 `DORMANT` 状态下，如果保留时间 `ri(t)` 到期，节点将进入 `RELEASING` 状态，并最终被 `ScalerWorker` 释放。
    5.  **永久状态 (PERMANENT)**:
        *   如果一个节点的保留时间 `ri(t)` 因为频繁被触发而超过了一个预设的阈值 `T_threshold`，它将进入 `PERMANENT` 状态（例如，持续一小时）。在此状态下，资源将保持活跃并被全网知晓，以缓解长期压力。

*   **验收标准**:
    1.  可以查询到每个弹性伸缩出的节点的当前状态（如 `DORMANT`, `TRIGGERED` 等）。
    2.  一个 `INACTIVE` 的节点在被触发伸缩后，其状态能正确地流转为 `SCALING_UP` -> `DORMANT`。
    3.  一个 `TRIGGERED` 的节点在闲置后，其保留时间 `ri(t)` 会随着时间推移而衰减。
    4.  频繁被触发的节点，其 `activity_score` 和保留时间 `ri(t)` 会显著增加。
    5.  处于 `DORMANT` 状态且保留时间到期的节点，会被系统自动标记为 `RELEASING` 并最终释放。

## 6. 非功能性需求
*   **性能需求**:
    *   路由决策API的响应时间应在 50ms 以内。
    *   代理节点的数据平面转发延迟应在 1ms 以内。
*   **可靠性需求**:
    *   系统应能容忍单个代理节点的故障，路由决策算法应能自动绕开失效节点。
    *   组长节点应有故障转移和手动切换机制。
*   **可配置性**:
    *   算法中的关键参数（如 `V_L`, `w_k^U`）应可通过配置文件进行调整。
    *   组长节点地址应可配置。

## 7. 待讨论/待明确事项
1.  **应用层协议定义**: 需要设计详细的、二进制优化的数据块头部格式和 Client-Proxy 之间的控制协议格式。
2.  **组长选举机制**: 当前为手动指定，未来版本可考虑引入基于分布式锁（如etcd）的自动选举机制。
3.  **弹性伸缩触发条件**: 需要量化定义触发弹性伸缩的具体阈值（例如，全网平均虚拟队列长度持续超过X达Y秒）。
4.  **安全模型**: 节点间的通信是否需要加密？Client 如何向代理节点进行身份验证？